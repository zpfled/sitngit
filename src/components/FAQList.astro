---
const { items } = Astro.props;
---
<div class="space-y-4">
  {items.map((item) => (
    <details class="bg-fog border border-clay/70 rounded-xl p-5">
      <summary class="cursor-pointer font-semibold text-ink">{item.q}</summary>
      <p class="mt-3 text-sm text-ink/75">{item.a}</p>
    </details>
  ))}
</div>
